using UnityEngine;
using TMPro;

public class GameOverPanel : MonoBehaviour {
    ////public TMP_Text scoreText;
    //public TMP_Text finalScoreText; // Reference to the UI text component for final score
    //public TMP_Text highestScoreText; // Reference to the UI text component for highest score

    //private void OnEnable() {
    //    //int playerScore = GameManager.Instance.GetPlayerScore();
    //    //scoreText.text = "Score: " + playerScore.ToString();
    //    //finalScoreText.text = "Final Score: " + playerScore.ToString();

    //    if (GameManager.Instance.IsGameOver()) {
    //        highestScoreText.gameObject.SetActive(true);
    //       // int highestScore = GameManager.Instance.GetHighestScore();
    //        highestScoreText.text = "Highest Score: " + highestScore.ToString();
    //    } else {
    //        highestScoreText.gameObject.SetActive(false);
    //    }
    //}
}
